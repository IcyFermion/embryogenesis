- Full tree with t <= 248 cutoff, without pruning of “fake” terminal nodes
    - real lineage: 4294.75
    - optimal re-assignment of the 248 terminal nodes: 4210.19
    - top-down greedy optimization:
        - optimize layer by layer, at each layer, find best node assignment using **only the nodes from current layer**: 4126.57
        - optimize layer by layer, at each layer, find best node assignment using **all non-terminal nodes**: 4762.16
        - remove tree structure limitation, just keep it as binary tree, select best next node to grow at any empty leaf position: 1880.32
    - bottom-up greedy optimization:
        - optimize layer by layer, at each layer, find best node assignment using **all non-terminal nodes**: 2926.74
- Retroactively pruned tree that removed all “fake” terminal nodes. **Pruned nodes are not considered for subsequent optimization assignment.**
    - real lineage: 2767.48
    - optimal re-assignment of the 248 terminal nodes: 2682.92
    - top-down greedy optimization:
        - optimize layer by layer, at each layer, find best node assignment using **only the nodes from current layer**: 2498.16
        - optimize layer by layer, at each layer, find best node assignment using **all non-terminal nodes**: 3382.47
    - bottom-up greedy optimization:
        - optimize layer by layer, at each layer, find best node assignment using **all non-terminal nodes:** 2834.95